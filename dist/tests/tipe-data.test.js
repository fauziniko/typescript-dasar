"use strict";
describe('tipe-data', () => {
    it('Should be able to create a new tipe data.', () => {
        let name = 'name';
        let balance = 0;
        let isVip = false;
        console.info(name);
        console.info(isVip);
        console.info(balance);
    });
});
